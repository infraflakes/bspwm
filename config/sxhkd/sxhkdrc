##Programs
#Terminal
super + Return
    ghostty

#Screenshot
super + shift + s
    scrot -s ~/Pictures/Screenshots/%Y-%m-%d-%s.png

#Launcher
super + space
    rofi -show drun

#Overview
super + a
    rofi -show window

##Reload config
super + r
    pkill -USR1 -x sxhkd

##Window control
#Quit BSPWM
super + Escape
    bspc quit

#Pin window
super + l
    slock

#Close and kill 
super + {_,shift + }q
    bspc node -{c,k}

##Focus
#Tiled/Monocle
super + m
    bspc desktop -l next

#Window state
super + {t,shift + t,w,f}
    bspc node -t {~tiled,~pseudo_tiled,~floating,~fullscreen}

########## DESKTOPS ##########
# switch to desktop 1-10
super + {1-9,0}
	bspc desktop -f '^{1-9,10}'

# send window to desktop 1-10
super + ctrl + {1-9,0}
	bspc node -d '^{1-9,10}'

########## FOCUS ##########
# focus next/prend window (on current desktop)
super + {Down,Up}
	bspc node -f next.local
super + Up
	bspc node -f prev.local

########## DESKTOP CYCLE ##########
# go to prev/next desktop
super + Left
	bspc desktop -f prev
super + Right
	bspc desktop -f next

########## MOVE WINDOW ALONG DESKTOPS ##########
# send window to previous / next desktop
super + ctrl + Left
	bspc node -d prev
super + ctrl + Right
	bspc node -d next

########## RESIZE (tiled windows) ##########
# expand border by 20 px
super + shift + {Left,Down,Up,Right}
	bspc node -z {left -20 0,bottom 0 20,top 0 -20,right 20 0}

########## MEDIA (XF86 hardware keys) ##########
# brightness
XF86MonBrightnessDown
	brightnessctl set 5%-
XF86MonBrightnessUp
	brightnessctl set 5%+

# volume
XF86AudioLowerVolume
	wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%-
XF86AudioRaiseVolume
	wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%+
XF86AudioMute
	wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
